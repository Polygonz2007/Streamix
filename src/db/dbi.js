
// High level interface for the database

export function creator(name, type) {
    // Add / get type

    // Add / get creator

}

export function collection(name, type, creator_id) {
    // Validate creator

    // Add / get type

    // Add / get collection

}

export function genre(name) {
    // Add / get genre

}

export function track(name, number, disc, collection_id, creators, genres) {
    // Add track

    // Loop over and add / get creators

    // Loop over and add / get genres

}
